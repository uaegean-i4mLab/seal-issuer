import { ObjectKeeper } from './types';
import { Identity } from 'jolocom-lib/js/identity/identity';
import { JolocomSDK } from 'src';
import { QueryOptions } from './storage';
export interface IdentityQuery {
    did?: string;
}
export declare class IdentityKeeper implements ObjectKeeper<Identity, {}, IdentityQuery> {
    protected sdk: JolocomSDK;
    constructor(sdk: JolocomSDK);
    /**
     * Retrieves a Signed Credential by id, or throws
     *
     * @param credParams - credential attributes
     * @returns SignedCredential instance
     * @category Credential Management
     */
    get(did: string): Promise<never>;
    find(attrs?: IdentityQuery, options?: QueryOptions): Promise<never[]>;
    delete(attrs?: IdentityQuery): Promise<boolean>;
}
